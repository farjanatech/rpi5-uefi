name: Fan exp.5B SOCB mailbox isolation (retired matrix)

on:
  workflow_dispatch:

# The exp.5B ACPI ownership gate already passed against functional source
# 6d8607979eaa2789db95758f8618bfe14fa72ebf in run 32157027540.
# This matrix workflow is retired because one Ubuntu runner stalled in apt while
# the parallel Debug runner completed successfully. Publication continues via
# the pinned release-only workflow.

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; use fan-exp5b-release.yml"
