name: Build Wi-Fi SDIO2 50MHz strap diagnostic UEFI

on:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/wifi-sdio2-50mhz-diag-controller.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-diagnostic:
    uses: farjanatech/rpi5-uefi/.github/workflows/wifi-sdio2-50mhz-diag.yml@diag/wifi-sdio2-50mhz-strap-off-exp5b
