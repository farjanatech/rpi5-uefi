name: Build Wi-Fi SDIO2 WL_ON reset diagnostic UEFI

on:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/wifi-sdio2-wlon-reset-diag-controller.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-diagnostic:
    uses: farjanatech/rpi5-uefi/.github/workflows/wifi-sdio2-wlon-reset-diag.yml@diag/wifi-sdio2-wlon-reset-exp5b
